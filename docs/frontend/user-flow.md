# User Flow — VERTEXworkout
**المرحلة 3 من 18 — UX Flow Documentation**
**الإصدار:** 1.0

---

## 0. مبادئ UX المتّبعة عبر كل الوثيقة

- **Progressive Disclosure:** لا نعرض للمستخدم إلا ما يحتاجه في كل خطوة، مع تأجيل التعقيد لمن يريده فعلاً.
- **Fail Gracefully:** كل نقطة فشل محتملة (دفع، تحقق، شبكة) لها مسار واضح للعودة والمحاولة، وليست طريقًا مسدودًا.
- **Guest-First حيث ممكن:** تصفح المتجر والمكتبة والأكاديمية لا يتطلب تسجيل دخول إجباري — التسجيل يُطلب فقط عند نقطة القيمة الحقيقية (الدفع، حفظ التقدم).
- **Consistency عبر الأدوار:** نفس مكونات الحالة (Loading/Error/Empty/Success) تُستخدم في كل الأدوار (Visitor/Client/Coach/Admin) لتقليل الحمل المعرفي.
- **RTL-First:** كل رحلة مصمّمة لتعمل بنفس الجودة بالعربية (RTL) والإنجليزية (LTR) دون أي فرق في التجربة.

---

## 1. رحلة كل نوع مستخدم (User Journeys by Persona)

### أ. الزائر (Visitor)
```mermaid
flowchart LR
    A[الوصول للموقع عبر بحث/إعلان] --> B[الصفحة الرئيسية]
    B --> C[تصفح: المتجر / البرامج / الأكاديمية / المدونة]
    C --> D{مهتم بالتسجيل؟}
    D -->|نعم| E[Authentication Flow]
    D -->|لا| F[استمرار التصفح كضيف]
    E --> G[Onboarding Flow]
```

### ب. المتدرب (Client)
```mermaid
flowchart LR
    A[تسجيل دخول] --> B[لوحة تحكم المتدرب]
    B --> C[متابعة برنامج نشط]
    B --> D[تصفح مكتبة التمارين]
    B --> E[الشراء من المتجر]
    B --> F[متابعة تقدمه الشخصي]
```

### ج. المدرب (Coach) — Phase 3
```mermaid
flowchart LR
    A[تسجيل دخول Coach] --> B[لوحة تحكم المدرب]
    B --> C[متابعة عملائه]
    B --> D[تخصيص برامج]
    B --> E[إدارة الحجوزات]
```

### د. الأدمن (Admin) — Phase 3
```mermaid
flowchart LR
    A[تسجيل دخول Admin] --> B[لوحة تحكم شاملة]
    B --> C[إدارة المحتوى والمنتجات]
    B --> D[إدارة المستخدمين والأدوار]
    B --> E[التقارير وسجلات التدقيق]
```

---

## 2. تدفق المصادقة (Authentication Flow)

```mermaid
flowchart TD
    A[زائر] --> B{لديه حساب؟}
    B -->|نعم| C[صفحة تسجيل الدخول]
    B -->|لا| D[صفحة إنشاء حساب]

    C --> E{طريقة الدخول}
    E -->|Email + Password| F[إدخال البيانات]
    E -->|Google Social Login| G[Supabase OAuth]
    F --> H{بيانات صحيحة؟}
    H -->|لا| I[Error State: بيانات غير صحيحة]
    I --> F
    H -->|نعم| J[توجيه حسب الدور - RBAC]
    G --> J

    C --> K[نسيت كلمة المرور؟]
    K --> L[إدخال البريد الإلكتروني]
    L --> M[إرسال رابط إعادة التعيين]
    M --> N[صفحة تعيين كلمة مرور جديدة]
    N --> C

    D --> O[الاسم، البريد، كلمة المرور]
    O --> P{تحقق Zod + CAPTCHA}
    P -->|فشل| Q[عرض أخطاء الحقول تحت كل حقل]
    Q --> O
    P -->|نجاح| R[إرسال بريد تأكيد]
    R --> S[Onboarding Flow]
```

**أفضل ممارسات UX مُطبَّقة:**
- رسائل الخطأ **تحت كل حقل مباشرة**، وليست رسالة عامة أعلى الفورم (تقليل زمن اكتشاف الخطأ).
- زر "نسيت كلمة المرور" ظاهر دائمًا بجانب حقل كلمة المرور، وليس مخفيًا.
- CAPTCHA يظهر فقط عند الاشتباه (بعد محاولة فاشلة أو سلوك مشبوه) وليس في كل مرة — تقليل الاحتكاك.

---

## 3. تدفق الإعداد الأولي (Onboarding Flow)

```mermaid
flowchart TD
    A[تسجيل ناجح] --> B[شاشة ترحيب: أهلاً بك في VERTEX]
    B --> C[اختيار الهدف: خسارة وزن / تضخيم / لياقة عامة / قوة وظيفية]
    C --> D[بيانات أساسية: الطول، الوزن، العمر، الجنس]
    D --> E[مستوى الخبرة: مبتدئ / متوسط / متقدم]
    E --> F{اقتراح برنامج مناسب}
    F -->|عرض اقتراحات| G[برامج مقترحة حسب الهدف والمستوى]
    F -->|تخطي الآن| H[لوحة تحكم المتدرب مباشرة]
    G --> H
```

**ملاحظة UX:** كل خطوة قابلة للتخطي (Skip) ما عدا البيانات الأساسية — نحترم وقت المستخدم ولا نفرض عليه رحلة طويلة إجبارية قبل الوصول للمنصة.

---

## 4. تدفق شراء من المتجر (Store Purchase Flow)

```mermaid
flowchart TD
    A[تصفح المتجر] --> B[صفحة تفاصيل منتج]
    B --> C{تسجيل دخول؟}
    C -->|لا| D[إضافة كضيف - Guest Cart]
    C -->|نعم| E[إضافة للسلة المرتبطة بالحساب]
    D --> F[سلة التسوق]
    E --> F
    F --> G{متابعة الشراء؟}
    G -->|لا| A
    G -->|نعم| H{هل هو ضيف؟}
    H -->|نعم| I[تسجيل دخول سريع أو إنشاء حساب]
    I --> J[صفحة الدفع - Checkout]
    H -->|لا| J
    J --> K[اختيار/إضافة عنوان الشحن]
    K --> L[اختيار بوابة الدفع: Paymob / Stripe]
    L --> M{نجاح الدفع؟}
    M -->|فشل| N[Error State + سبب الفشل + إعادة المحاولة]
    N --> L
    M -->|نجاح| O[صفحة تأكيد الطلب + إشعار]
    O --> P[تتبع حالة الطلب]
```

**ملاحظة UX:** سلة الضيف (Guest Cart) تُحفظ محليًا وتُدمج تلقائيًا مع حساب المستخدم بمجرد تسجيل الدخول — لا يفقد المستخدم اختياراته أبدًا.

---

## 5. تدفق الاشتراك في برنامج تدريبي (Program Enrollment Flow)

```mermaid
flowchart TD
    A[تصفح البرامج] --> B[فلترة: الهدف / المستوى / المدة]
    B --> C[صفحة تفاصيل البرنامج]
    C --> D{مجاني أم مدفوع؟}
    D -->|مجاني| E[اشتراك مباشر بضغطة واحدة]
    D -->|مدفوع| F[الانتقال لصفحة الدفع]
    F --> G{نجاح الدفع؟}
    G -->|فشل| F
    G -->|نجاح| E
    E --> H[إضافة سجل Enrollment]
    H --> I[ظهور البرنامج في لوحة تحكم المتدرب]
    I --> J[بدء أول يوم تدريبي]
```

---

## 6. تدفق مكتبة التمارين (Exercise Library Flow)

```mermaid
flowchart TD
    A[دخول مكتبة التمارين] --> B[بحث / فلترة: عضلة، معدة، هدف، صعوبة]
    B --> C{نتائج موجودة؟}
    C -->|لا| D[Empty State: لا نتائج + اقتراح تعديل الفلاتر]
    C -->|نعم| E[شبكة نتائج التمارين]
    E --> F[فتح تفاصيل تمرين]
    F --> G[فيديو + شرح + عضلات مستهدفة + أخطاء شائعة + نصائح]
    G --> H{إضافة للمفضلة أو برنامج شخصي؟}
    H -->|نعم| I[حفظ + إشعار تأكيد]
    H -->|لا| B
```

---

## 7. تدفق VERTEX Academy

```mermaid
flowchart TD
    A[دخول VERTEX Academy] --> B[اختيار قسم: Nutrition / Anatomy / Mobility / Recovery / Injury Prevention]
    B --> C[عرض Smart Cards والمقالات]
    C --> D[فتح مقال]
    D --> E[قراءة المحتوى + وسائط مرفقة]
    E --> F[محتوى ذو صلة عبر الوسوم المشتركة]
    F --> G{استكشاف المزيد؟}
    G -->|نعم| B
    G -->|لا| H[العودة للوحة التحكم]
```

---

## 8. تدفق لوحة تحكم المتدرب (Client Dashboard Flow)

```mermaid
flowchart TD
    A[تسجيل الدخول كـ Client] --> B[لوحة التحكم الرئيسية]
    B --> C[ملخص التقدم: الوزن / القياسات / الإنجازات]
    B --> D[برامجي المشتركة]
    B --> E[سجل الطلبات]
    B --> F[الإشعارات]
    B --> G[الإعدادات الشخصية]
    D --> H[متابعة برنامج نشط]
    H --> I[تسجيل إتمام تمرين اليوم]
    I --> J[تحديث Activity Timeline + احتمال إنجاز جديد]
```

---

## 9. تدفق لوحة تحكم المدرب (Coach Dashboard Flow) — Phase 3

```mermaid
flowchart TD
    A[تسجيل دخول Coach] --> B[لوحة تحكم المدرب]
    B --> C[قائمة عملائه المرتبطين]
    C --> D[عرض تقدم عميل محدد]
    D --> E[تخصيص/تعديل برنامج العميل]
    B --> F[إدارة جدول الحجوزات]
    B --> G[إشعارات ورسائل العملاء]
```

---

## 10. تدفق لوحة تحكم الأدمن (Admin Dashboard Flow) — Phase 3

```mermaid
flowchart TD
    A[تسجيل دخول Admin] --> B[لوحة التحكم الرئيسية]
    B --> C[إدارة المستخدمين والأدوار]
    B --> D[إدارة المنتجات والمخزون]
    B --> E[إدارة البرامج والتمارين والمحتوى]
    B --> F[إدارة الطلبات وحالتها]
    B --> G[التقارير والتحليلات]
    B --> H[سجلات التدقيق - Audit Logs]
```

---

## 11. تدفق الصلاحيات (Permission Flow — RBAC)

```mermaid
flowchart TD
    A[طلب وصول لصفحة أو مورد] --> B{مسجل دخول؟}
    B -->|لا| C[توجيه لصفحة تسجيل الدخول]
    B -->|نعم| D[جلب أدوار المستخدم من user_roles]
    D --> E{يملك الدور الصلاحية المطلوبة؟}
    E -->|لا| F[صفحة 403 - غير مصرح بالوصول]
    E -->|نعم| G[عرض المحتوى/المورد المطلوب]
```

**ملاحظة تصميمية:** يتم التحقق من الصلاحيات على مستويين: **Middleware** (منع الوصول للمسار كليًا) و**مكوّن الواجهة** (إخفاء أزرار/إجراءات لا يملك المستخدم صلاحيتها) — حماية مزدوجة (Defense in Depth).

---

## 12. تدفق تعدد اللغات (Multi-language Flow)

```mermaid
flowchart TD
    A[زيارة الموقع] --> B{لغة محفوظة مسبقًا؟}
    B -->|نعم| C[تحميل اللغة المحفوظة]
    B -->|لا| D[كشف لغة المتصفح]
    D --> E{اللغة المكتشفة عربية؟}
    E -->|نعم| F[تحميل ar + اتجاه RTL]
    E -->|لا| G[الافتراضي: en + اتجاه LTR]
    C --> H[عرض الواجهة]
    F --> H
    G --> H
    H --> I{تبديل يدوي من المستخدم؟}
    I -->|نعم| J[حفظ التفضيل + إعادة تحميل بالاتجاه الصحيح]
    J --> H
```

---

## 13. الحالات العامة عبر كل الواجهات (Global UI States)

كل مكوّن يجلب بيانات في المنصة (منتج، تمرين، برنامج، إشعارات...) يمر بنفس دورة الحالات الأربع:

```mermaid
flowchart LR
    A[بدء الطلب] --> B[Loading State]
    B --> C{نجاح الاستجابة؟}
    C -->|نعم| D{توجد بيانات؟}
    D -->|نعم| E[Success State]
    D -->|لا| F[Empty State]
    C -->|لا| G[Error State]
```

| الحالة | أفضل الممارسات المتبعة |
|---|---|
| **Loading** | Skeleton Screens (وليس Spinner فقط) تحاكي شكل المحتوى الفعلي — تقلل الإحساس بزمن الانتظار |
| **Empty** | رسالة ودّية + توضيح السبب + إجراء مقترح (مثال: "لا توجد منتجات بهذا الفلتر، جرّب تعديل البحث") — ليست شاشة فارغة بلا سياق |
| **Error** | رسالة واضحة بلغة الإنسان (لا رموز أخطاء تقنية) + زر "إعادة المحاولة" دائمًا متاح |
| **Success** | تأكيد بصري فوري (Toast/Checkmark) + خطوة تالية واضحة (مثال: بعد الشراء، زر "تتبع الطلب") |

---

## 14. الفروقات بين تجربة الموبايل والديسكتوب

| العنصر | Desktop | Mobile |
|---|---|---|
| التنقل الرئيسي | Header أفقي ثابت مع كل الأقسام ظاهرة | Bottom Navigation Bar (الرئيسية، المتجر، الأكاديمية، حسابي) |
| الفلاتر (مكتبة التمارين/المتجر) | Sidebar جانبي دائم الظهور | Bottom Sheet قابل للسحب لأعلى |
| تفاصيل المنتج/التمرين | عرض جنبًا إلى جنب (صورة + تفاصيل) | عرض عمودي متتابع (Scroll) |
| لوحة التحكم | Sidebar ثابت + محتوى رئيسي | قائمة منسدلة (Hamburger) + محتوى ملء الشاشة |
| الفيديوهات (Exercise Library) | تشغيل داخل الصفحة (Inline) | تشغيل ملء الشاشة تلقائيًا عند الضغط |

كل الرحلات المذكورة أعلاه **مصمَّمة لتعمل بنفس المنطق على كلا الجهازين**، مع اختلاف فقط في طريقة عرض التنقل والتفاعل، وليس في تسلسل الخطوات نفسه.

---

## ✅ يرجى المراجعة والموافقة على:
- [ ] رحلات الأدوار الأربعة (Visitor/Client/Coach/Admin)
- [ ] تدفق المصادقة (Login/Register/Forgot Password/Social Login)
- [ ] تدفق Onboarding
- [ ] تدفق الشراء وتدفق الاشتراك في البرامج
- [ ] تدفق مكتبة التمارين وVERTEX Academy
- [ ] تدفقات لوحات التحكم الثلاث
- [ ] تدفق الصلاحيات (RBAC) وتدفق تعدد اللغات
- [ ] الحالات العامة (Loading/Empty/Error/Success)
- [ ] الفروقات بين الموبايل والديسكتوب

بعد الموافقة، ننتقل مباشرة إلى **المرحلة 4: Sitemap**.
